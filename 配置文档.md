# AlphaGPT 配置文档

本文档说明如何在本地配置并启动 AlphaGPT 的主要模块（数据管道、模型训练、策略执行、可视化面板）。

## 1. 环境要求

- Python 3.10+（建议 3.10 或 3.11）
- PostgreSQL 14+
- 可选：NVIDIA GPU（用于加速 `model_core` 训练）
- 可访问外部 API：Birdeye、DexScreener、Solana RPC、Jupiter

## 2. 快速安装

在项目根目录执行：

```bash
python -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install python-dotenv loguru pandas sqlalchemy asyncpg torch tqdm streamlit plotly requests solders base58
```

> 说明：仓库未包含统一 `requirements.txt`，以上为按源码导入推导出的最小依赖集合。

## 3. `.env` 配置

在项目根目录新建 `.env`：

```bash
# PostgreSQL
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_quant

# Data Pipeline
BIRDEYE_API_KEY=your_birdeye_api_key

# Execution / Solana
QUICKNODE_RPC_URL=https://your-solana-rpc-url
SOLANA_PRIVATE_KEY=your_base58_or_json_array_private_key
```

### 3.1 字段说明

#### 数据库（`data_pipeline/config.py`、`model_core/config.py`）
- `DB_USER` / `DB_PASSWORD` / `DB_HOST` / `DB_PORT` / `DB_NAME`：PostgreSQL 连接参数。
- 系统会拼接为 DSN/URL：
  - `postgresql://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME`

#### 数据管道（`data_pipeline/config.py`）
- `BIRDEYE_API_KEY`：Birdeye 接口密钥（必填）。
- 其他内置参数（需改源码）：
  - `CHAIN=solana`
  - `TIMEFRAME=1m`
  - `MIN_LIQUIDITY_USD=500000.0`
  - `MIN_FDV=10000000.0`
  - `CONCURRENCY=20`
  - `HISTORY_DAYS=7`

#### 交易执行（`execution/config.py`）
- `QUICKNODE_RPC_URL`：Solana RPC 节点。
- `SOLANA_PRIVATE_KEY`：钱包私钥（支持 Base58 或 JSON 数组）。
- 注意：`SOLANA_PRIVATE_KEY` 为空会直接抛错，程序无法启动。

#### 策略管理（`strategy_manager/config.py`）
当前为源码内常量，未接入环境变量：
- 最大持仓：`MAX_OPEN_POSITIONS=3`
- 单笔仓位：`ENTRY_AMOUNT_SOL=2.0`
- 止损：`STOP_LOSS_PCT=-0.05`
- 买入阈值：`BUY_THRESHOLD=0.85`
- 卖出阈值：`SELL_THRESHOLD=0.45`

## 4. 数据库初始化建议

先创建数据库：

```sql
CREATE DATABASE crypto_quant;
```

运行数据管道后会按代码逻辑写入所需表（如 `ohlcv`）。首次运行请确保连接账号有建表权限。

## 5. 启动流程

建议按以下顺序启动：

### 步骤 1：运行数据管道

```bash
python -m data_pipeline.run_pipeline
```

期望行为：
- 若未配置 `BIRDEYE_API_KEY`，会报错并退出。
- 若数据库正常可连接，将执行同步流程。

### 步骤 2：训练模型并生成策略文件

```bash
python -m model_core.engine
```

输出：
- 训练结束后会在项目根目录生成 `best_meme_strategy.json`。

### 步骤 3：启动策略执行（实盘逻辑）

> ⚠️ 涉及真实下单逻辑，建议先在测试钱包/小资金验证。

```bash
python -m strategy_manager.runner
```

前置条件：
- 已有 `best_meme_strategy.json`
- `.env` 中 Solana 相关参数正确

### 步骤 4：启动可视化面板

```bash
streamlit run dashboard/app.py
```

访问命令输出中的本地 URL（通常 `http://localhost:8501`）。

## 6. 常见问题排查

### 6.1 `Missing SOLANA_PRIVATE_KEY in .env`
- 原因：`execution/config.py` 在导入阶段检查私钥，缺失即抛错。
- 处理：补齐 `.env` 的 `SOLANA_PRIVATE_KEY`。

### 6.2 `BIRDEYE_API_KEY is missing in .env`
- 原因：数据管道入口显式校验 API Key。
- 处理：补齐 `.env` 的 `BIRDEYE_API_KEY`。

### 6.3 PostgreSQL 连接失败
- 检查数据库是否启动、账号权限、端口和防火墙。
- 使用以下命令快速验证：

```bash
python - <<'PY'
import os
from sqlalchemy import create_engine, text
url = f"postgresql://{os.getenv('DB_USER','postgres')}:{os.getenv('DB_PASSWORD','password')}@{os.getenv('DB_HOST','localhost')}:{os.getenv('DB_PORT','5432')}/{os.getenv('DB_NAME','crypto_quant')}"
engine = create_engine(url)
with engine.connect() as conn:
    print(conn.execute(text('select 1')).scalar())
PY
```

### 6.4 `best_meme_strategy.json` 不存在
- 原因：策略 runner 会在启动时读取该文件，缺失会退出。
- 处理：先执行 `python -m model_core.engine`。

## 7. 安全建议

- 不要提交 `.env`、私钥、RPC Token 到 Git。
- 强烈建议将交易私钥与开发环境隔离（专用低权限钱包）。
- 实盘前先用极小仓位验证滑点、成交、止损链路。
